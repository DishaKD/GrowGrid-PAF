server:
  port: 8090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/post_service?createDatabaseIfNotExist=true
    username: root
    password: 1111
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: false

file:
  upload-dir: uploads/
